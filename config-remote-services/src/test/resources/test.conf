remote-services-ok = {
  remote-services = [
    {
      service-id = "test-service"
      base-url = "http://localhost:45678"
      routes = [
        {route-id = "fetch-pets", method = "GET", path = "/pets"}
        {route-id = "fetch-pet", method = "GET", path = "/pets/{id}"}
      ]
    }
  ]
  gateway-rewrite-routes = [
    {gateway-path = "/custom-fetch-pet/{id}", route-id = "fetch-pet"}
  ]
}

remote-services-missing-base-url = {
  remote-services = [
    {
      service-id = "test-service"
      routes = [
        {route-id = "fetch-pets", method = "GET", path = "/pets"}
      ]
    }
  ]
}

remote-services-wrong-route-path = {
  remote-services = [
    {
      service-id = "test-service"
      base-url = "http://localhost:45678"
      routes = [
        {route-id = "fetch-pets", method = "GET", path = "pets"}
      ]
    }
  ]
}

gateway-rewrite-route-test = {
  remote-services = [
    {
      service-id = "test-service"
      base-url = "http://localhost:45678"
      routes = [
        {route-id = "fetch-pets", method = "GET", path = "/pets"}
      ]
    }
  ]
  gateway-rewrite-routes = [
    {gateway-path = "/pets", route-id = "route-a"}
  ]
}
